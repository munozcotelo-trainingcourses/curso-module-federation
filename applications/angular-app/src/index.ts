import { inicio } from "./ui";

const name: string = "angular-app";

setTimeout(() => {

    /*
     * comentar para federar 
     * descomentar para trabajo local
     */
    // inicio("angular-app");

}, 5000);

export { name };
